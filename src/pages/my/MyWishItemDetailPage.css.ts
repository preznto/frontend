import { style } from '@vanilla-extract/css';

const wishListInfoStyle = style({});

export const wishItemImageSection = style({
  position: 'relative',
});

export const wishItemImg = style({
  width: '100%',
  height: 393,
});

export const linkBtn = style({
  position: 'absolute',
  bottom: -2,
  left: 20,
});
